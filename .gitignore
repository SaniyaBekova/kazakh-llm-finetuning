# ── Data files (too large for GitHub, download via scraper) ──
data/raw/
data/processed/
*.jsonl
*.json
*.parquet
*.csv

# ── Model weights & checkpoints (too large, use HuggingFace Hub) ──
models/
*.bin
*.safetensors
*.pt
*.pth
checkpoints/

# ── Jupyter / IPython ──
.ipynb_checkpoints/
*/.ipynb_checkpoints/

# ── Python ──
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ──
.venv/
venv/
env/
.env

# ── Secrets & credentials (just in case) ──
.env
*.env
secrets.json
credentials.json
token.txt
*token*
*secret*

# ── Google Colab / Drive artifacts ──
.config/
drive/

# ── OS artifacts ──
.DS_Store
Thumbs.db
desktop.ini

# ── Logs & temp ──
*.log
tmp/
temp/
